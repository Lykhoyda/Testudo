# =============================================================================
# Testudo Environment Variables
# =============================================================================
# Copy this file to .env and adjust values as needed.
# DO NOT commit .env files with real credentials.
# =============================================================================

# -----------------------------------------------------------------------------
# API Server (apps/api)
# -----------------------------------------------------------------------------

# PostgreSQL connection string
# Note: Using port 5433 to avoid conflicts with local PostgreSQL installations
DATABASE_URL=postgresql://testudo:testudo_dev@localhost:5433/testudo

# API server port
PORT=3001

# -----------------------------------------------------------------------------
# Browser Extension (packages/extension)
# -----------------------------------------------------------------------------

# Threat Intelligence API URL
# - Local development: http://localhost:3001
# - Production: https://api.testudo.security
TESTUDO_API_URL=http://localhost:3001

# Safe Filter CDN URL (for bloom filter sync)
# - Local development: Leave empty to use hardcoded fallback
# - Production: https://cdn.testudo.security
TESTUDO_CDN_URL=

# -----------------------------------------------------------------------------
# Local Development with Docker
# -----------------------------------------------------------------------------
#
# Start PostgreSQL:
#   docker compose up -d
#
# Run migrations:
#   cd apps/api && yarn db:migrate
#
# Seed database:
#   cd apps/api && yarn db:seed
#
# Start API:
#   yarn dev:api
#
# Build extension:
#   yarn workspace @testudo/extension build
# -----------------------------------------------------------------------------
